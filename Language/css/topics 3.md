## 1️⃣ Basics of CSS

1. What is CSS and why is it used?
    
2. Difference between **inline, internal, and external CSS**.
    
3. Difference between **id (`#id`)** and **class (`.class`)** selectors.
    
4. Difference between **relative, absolute, fixed, and sticky** positioning.
    
5. Difference between **inline, block, and inline-block** elements.
    
6. Difference between **px, em, rem, %, vh, vw** units.
    
7. Difference between **absolute and relative URLs in CSS**.
    
8. What are **pseudo-classes** and **pseudo-elements**? (`:hover`, `:first-child`, `::before`, `::after`)
    

---

## 2️⃣ CSS Box Model

1. Explain the **box model** (content, padding, border, margin).
    
2. Difference between **content-box** and **border-box**.
    
3. Difference between **inline, block, and inline-block box behavior**.
    
4. How to remove default spacing (margin/padding) of elements.
    

---

## 3️⃣ Selectors

1. Difference between **element, class, id, and universal selectors**.
    
2. Difference between **descendant (`div p`)** and **child (`div > p`)** selectors.
    
3. Attribute selectors: `[type="text"]`, `[name^="a"]`, `[name$="z"]`.
    
4. Grouping selectors with `,` and chaining selectors.
    
5. Difference between **:nth-child() vs :nth-of-type()**.
    

---

## 4️⃣ CSS Positioning & Layout

1. Difference between **relative, absolute, fixed, and sticky** positions.
    
2. Difference between **static vs relative positioning**.
    
3. How **z-index** works.
    
4. How **float** works and what is **clearfix**.
    
5. Difference between **inline, inline-block, block, and flex** display types.
    
6. Difference between **absolute vs fixed** positioning.
    

---

## 5️⃣ Flexbox

1. What is **flexbox**? Advantages over floats.
    
2. Key properties: `display: flex`, `justify-content`, `align-items`, `flex-direction`, `flex-wrap`.
    
3. Difference between **justify-content** and **align-items**.
    
4. Difference between **flex-grow, flex-shrink, and flex-basis**.
    

---

## 6️⃣ Grid Layout

1. What is CSS Grid?
    
2. Difference between **Flexbox and Grid**.
    
3. Key properties: `display: grid`, `grid-template-columns`, `grid-template-rows`, `gap`, `grid-area`.
    
4. How to **position items using grid-row, grid-column**.
    

---

## 7️⃣ Colors, Backgrounds & Text

1. Difference between **hex, RGB, RGBA, HSL** color formats.
    
2. CSS **background properties**: `background-color`, `background-image`, `background-size`, `background-repeat`.
    
3. CSS **text properties**: `text-align`, `text-decoration`, `text-transform`, `letter-spacing`, `line-height`.
    
4. Difference between **relative, absolute, and fixed units in text** (px vs em vs rem).
    

---

## 8️⃣ Transitions, Animations & Transform

1. Difference between **transition** and **animation**.
    
2. Key CSS transition properties: `transition-property`, `transition-duration`, `transition-timing-function`.
    
3. Key CSS animation properties: `@keyframes`, `animation-name`, `animation-duration`.
    
4. CSS transforms: `scale()`, `rotate()`, `translate()`, `skew()`.
    

---

## 9️⃣ Responsive Design

1. What is **responsive design**?
    
2. Difference between **responsive vs adaptive design**.
    
3. Use of **media queries**: `@media screen and (max-width: 768px) { ... }`
    
4. Difference between **max-width and min-width** in media queries.
    
5. Difference between **vw, vh** and `%` units for responsive layout.
    

---

## 10️⃣ CSS Specificity & Inheritance

1. What is **CSS specificity**?
    
2. Order of specificity: Inline > ID > Class > Element.
    
3. Difference between **inheritance vs non-inheritance**.
    
4. What is **!important** and when to use it.
    

---

## 11️⃣ Common CSS Properties (High Frequency)

1. **Margin vs Padding**
    
2. **Overflow**: `visible`, `hidden`, `scroll`, `auto`
    
3. **Cursor** property and values: `pointer`, `default`, `text`
    
4. **Box-shadow** and **text-shadow**
    
5. **Opacity** vs **visibility**
    

---

## 12️⃣ Miscellaneous / Frequently Asked

1. Difference between **relative, fixed, sticky, absolute** (position recap).
    
2. Difference between **inline vs block vs inline-block** (layout recap).
    
3. Difference between **em vs rem vs px**.
    
4. Difference between **visibility: hidden** and **display: none**.
    
5. What is **z-index** and how stacking context works.
    
6. How **CSS specificity** works when multiple rules apply.
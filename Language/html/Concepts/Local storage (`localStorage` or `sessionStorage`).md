- Allows storing data **locally in the browser** for persistent or session use.
- `localStorage`→ Data persists even after closing the browser.
- `sessionStorage`→ Data cleared when the browser session ends.
```js
localStorage.setItem("name", "Anurag");
sessionStorage.setItem("temp", "123");
```
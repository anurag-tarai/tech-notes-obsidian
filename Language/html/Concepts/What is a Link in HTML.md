A **link** allows users to **navigate from one page to another**, to a section on the same page, or to an external resource.

HTML links are created using the **`<a>` (anchor) tag**.

```html
<a href="https://example.com">Visit Example</a>
```
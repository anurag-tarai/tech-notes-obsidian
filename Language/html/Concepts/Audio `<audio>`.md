Used to embed audio files like mp3, wav.
```html
<audio src="song.mp3" controls autoplay loop></audio>
```

**Common attributes:** 
- `controls` → Shows play/pause controls
- `autoplay` → Starts playing automatically
- `loop` → Repeats audio continuously

> Note: Auto-play may be blocked by browsers unless muted or user-initiated.
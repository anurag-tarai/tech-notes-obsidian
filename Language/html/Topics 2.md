## 1️⃣ Basics of HTML

1. What is HTML?
    
2. Difference between HTML and HTML5.
    
3. Difference between **HTML, CSS, and JavaScript**.
    
4. What is a **tag**, **element**, and **attribute**?
    
5. Difference between **block-level** and **inline elements**.
    
6. Difference between **empty/self-closing tags** and **normal tags**.
    
7. Difference between **HTML and XHTML**.
    
8. What is the **`<!DOCTYPE html>`** declaration?
    

---

## 2️⃣ HTML Document Structure

1. Basic structure of an HTML document (`<!DOCTYPE html>`, `<html>`, `<head>`, `<body>`).
    
2. Purpose of `<head>` tag.
    
3. Purpose of `<title>`, `<meta>`, `<link>`, and `<style>` tags.
    
4. Difference between `<div>` and `<span>`.
    
5. Difference between **relative** and **absolute paths** for links and images.
    

---

## 3️⃣ Semantic HTML

1. What are **semantic tags** and why are they important?
    
2. Difference between **semantic** and **non-semantic tags**.
    
3. Explain the use of:
    
    - `<header>`, `<nav>`, `<main>`
        
    - `<section>`, `<article>`
        
    - `<aside>`, `<footer>`
        
    - `<figure>`, `<figcaption>`
        
4. How semantic HTML improves **SEO**.
    
5. How semantic tags help **accessibility**.
    

---

## 4️⃣ HTML Forms

1. What is an HTML form and its purpose?
    
2. Explain `action` and `method` attributes.
    
3. Difference between **GET** and **POST** methods.
    
4. Common form elements: `<input>`, `<textarea>`, `<select>`, `<option>`, `<button>`.
    
5. Input types: `text`, `password`, `email`, `number`, `radio`, `checkbox`, `file`, `hidden`.
    
6. Form attributes: `name`, `id`, `placeholder`, `required`, `readonly`, `disabled`.
    
7. What is **browser validation**? How does it work?
    
8. How does **form data reach the backend**?
    

---

## 5️⃣ Media & Assets

1. How to include images using `<img>` and importance of `alt` attribute.
    
2. How to embed audio using `<audio>` and its attributes (`controls`, `autoplay`, `loop`).
    
3. How to embed video using `<video>` and its attributes (`controls`, `autoplay`, `loop`, `poster`). 
    
4. How to embed external content using `<iframe>` and its basic attributes (`src`, `title`, `allowfullscreen`).
    

---

## 6️⃣ Links & Navigation

1. Difference between **absolute** and **relative links**.
    
2. Difference between `<a>` tag and button click for navigation.
    
3. What is a **mailto** link?
    
4. What are **anchor links (#)**?
    

---

## 7️⃣ Lists & Tables

1. Difference between `<ul>`, `<ol>`, `<li>`.
    
2. Difference between **ordered** and **unordered lists**.
    
3. Table tags: `<table>`, `<tr>`, `<td>`, `<th>`, `<caption>`.
    
4. Difference between `<thead>`, `<tbody>`, `<tfoot>`.
    

---

## 8️⃣ HTML5 New Features

1. New semantic tags in HTML5. 
    
2. New input types in HTML5 forms (`email`, `url`, `date`, `number`, `range`). 
    
3. Multimedia support: `<audio>`, `<video>`.
    
4. `<canvas>` and `<svg>` basic idea.
    
5. Local storage (`localStorage` / `sessionStorage`) – basic understanding.
    
6. Offline support & web workers (basic understanding).
    

---

## 9️⃣ Attributes & Global Concepts

1. Difference between **id** and **class**.
    
2. Difference between **inline**, **internal**, and **external CSS**.
    
3. Common global attributes: `id`, `class`, `title`, `style`, `hidden`, `contenteditable`.
    
4. What is the difference between **relative units** (%, em, rem) and **absolute units** (px, pt).
    

---

## 10️⃣ Miscellaneous / Frequently Asked

1. Difference between **HTML and HTML5 doctype**.
    
2. Difference between `<strong>` and `<b>`; `<em>` and `<i>`.
    
3. Difference between `<br>` and `<p>` tag.
    
4. What is **favicon** and how to add it?
    
5. Difference between **span** and **div**.
    
6. Difference between **HTML tags** and **HTML elements**.
    
7. Difference between `<script>` in `<head>` vs `<body>`.
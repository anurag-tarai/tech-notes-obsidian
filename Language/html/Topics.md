## PART 1: HTML (Must-Know)

### 1. HTML Basics (Quick revision)

- What is HTML? (structure, not styling)
    
- Tags vs elements
    
- Attributes
    
- Block vs inline elements
    

👉 Interview focus:

- Why HTML is not a programming language
    
- Difference between `<div>` and `<span>`
    

---

### 2. Semantic HTML (VERY IMPORTANT)

Learn **why semantics matter**, not just tags.

Must-know tags:

- `<header>`, `<nav>`, `<main>`
    
- `<section>`, `<article>`
    
- `<aside>`, `<footer>`
    
- `<figure>`, `<figcaption>`
    

👉 Interview questions:

- Why semantic HTML improves SEO?
    
- How semantic tags help accessibility?
    

---

### 3. Forms & Inputs (Backend-Relevant)

Since you’re backend-leaning, this is critical.

- `<form>` attributes: `action`, `method`
    
- GET vs POST
    
- `<input>` types:
    
    - text, email, password
        
    - number, date
        
    - checkbox, radio, file
        
- `<label>` and `for`
    
- `required`, `pattern`, `disabled`, `readonly`
    

👉 Interview angle:

- How browser validation works
    
- How form data reaches backend
    

---

### 4. Media & Assets

- `<img>` (`alt` attribute – accessibility!)
    
- `<audio>`, `<video>`
    
- `<iframe>` (basic use)
    

---

### 5. HTML Best Practices

- Proper nesting
    
- Avoiding `<br>` for spacing
    
- Use semantic tags over `<div>`
    
- Accessibility basics (ARIA – just intro)
## ✅ **Most Asked DOM Interview Questions**

### **1. What is the DOM?**

Explain what DOM is and why it exists.

---

### **2. What is a DOM Tree?**

Structure of the webpage represented as a tree of nodes.

---

### **3. What is the `document` object?**

Why it is the entry point to the DOM.

---

### **4. Difference between HTML and DOM?**

Very commonly asked.

---

### **5. What are DOM Nodes?**

Types: element, text, comment, attribute nodes.

---

### **6. What is the difference between Node and Element?**

Node = generic  
Element = specific HTML tags

---

### **7. How to select elements in the DOM?**

- `getElementById`
    
- `querySelector`
    
- `querySelectorAll`
    
- class, tag selectors
    

---

### **8. Difference between `querySelector` and `getElementById`?**

Key differences in speed, selector flexibility, return types.

---

### **9. How to create, add, or remove DOM elements?**

- `document.createElement()`
    
- `.append()`
    
- `.prepend()`
    
- `.remove()`
    

---

### **10. How to update text in the DOM?**

- `textContent`
    
- `innerText`
    
- `innerHTML` (and why it's dangerous)
    

---

### **11. What is the difference between `textContent`, `innerText`, and `innerHTML`?**

One of the most asked.

---

### **12. How do event listeners work?**

- `addEventListener()`
    
- Event bubbling & capturing
    

---

### **13. What is Event Bubbling and Event Capturing?**

Super common in interviews.

---

### **14. What is Event Delegation?**

Interviews love this question.

---

### **15. How does the browser convert HTML to DOM?**

DOM construction process.

---

### **16. What is reflow and repaint in DOM?**

How layout and rendering work.

---

### **17. What is the difference between == modifying the DOM directly vs using Virtual DOM (React)?**

Conceptual question.
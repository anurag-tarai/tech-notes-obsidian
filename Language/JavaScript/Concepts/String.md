Strings are sequences of characters enclosed in **single `' '`**, **double `" "`**, or **backticks `` ` ` ``** (template literals).
```js
let str1 = "Hello";
let str2 = 'World';
let str3 = `Hello World`; // template literal
```
Removes **whitespace** from **both ends** of a string.
```js
let str = "   Hello World   ";
console.log(str.trim()); // "Hello World"
```

**Variants:**
- `trimStart()` / `trimLeft()` → removes only leading spaces
- `trimEnd()` / `trimRight()` → removes only trailing spaces
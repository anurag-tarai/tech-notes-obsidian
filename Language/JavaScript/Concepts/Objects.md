An **object** in JavaScript is a **collection of key–value pairs**.
- These keys are called **properties**, and their values can be **anything**—numbers, strings, arrays, functions, or even other objects.

Example :
```js
const person = {
  name: "Anurag",
  age: 22,
  isStudent: true
	};
```
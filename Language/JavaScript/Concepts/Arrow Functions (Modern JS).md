Shorter syntax and used commonly.

```js
const add = (a, b) => a + b;

console.log(add(2, 3)); // 5
```

### If function has multiple lines:

```js
const greet = () => {
  console.log("Hello!");
};
```

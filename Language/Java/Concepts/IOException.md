**`IOException`** is a **checked exception** in Java that occurs when an **input/output operation fails or is interrupted**.

In simple words:
> It happens when Java **cannot read from or write to a file or stream**.

Package
```java
java.io
```

➡️ Must be **handled** using `try-catch` or `throws`
A **marker interface** is an interface that **does not contain any methods**.  
It is used to **mark a class** so that the **JVM or API can apply special behavior** to that class.

In short:

> A marker interface gives **metadata** to the JVM or compiler.
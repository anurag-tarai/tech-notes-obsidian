> Generics means parameterized types.

- They allows us to write code that works with different data types using a single class, interface or method.
- Instead of writing separate code for each type, we use type parameters like `<T>` to make the code reusable.
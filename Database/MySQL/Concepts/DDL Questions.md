# DDL Practice Questions (30)

### Table Creation

1. Create a table **Employee** with columns id, name, salary, department.
    
2. Create a table **Department** with department_id and department_name.
    
3. Create a table with **PRIMARY KEY** constraint.
    
4. Create a table with **NOT NULL constraint** on name.
    
5. Create a table with **UNIQUE constraint** on email.
    
6. Create a table with **DEFAULT value** for salary.
    
7. Create a table with **CHECK constraint** to ensure salary > 0.
    
8. Create a table with **AUTO_INCREMENT id**.
    
9. Create a table **Project** with project_id, project_name, start_date.
    
10. Create a table **Employee_Project** for many-to-many relation.
    

---

### Foreign Key Relationships

11. Create a table Employee with **Foreign Key referencing Department**.
    
12. Create a table Orders referencing Customer table.
    
13. Create a table Student referencing Course table.
    
14. Create a table with **ON DELETE CASCADE**.
    
15. Create a table with **ON UPDATE CASCADE**.
    

---

### Alter Table

16. Add a new column **email** to Employee table.
    
17. Add a new column **date_of_joining**.
    
18. Modify column **salary datatype**.
    
19. Rename column **name to employee_name**.
    
20. Drop column **email** from table.
    

---

### Constraints Modification

21. Add **PRIMARY KEY** to an existing table.
    
22. Add **UNIQUE constraint** to a column.
    
23. Add **FOREIGN KEY constraint** to existing table.
    
24. Remove **FOREIGN KEY constraint**.
    
25. Drop **UNIQUE constraint**.
    

---

### Table Management

26. Rename table **Employee to Employees**.
    
27. Delete all rows without deleting table structure.
    
28. Drop table **Project**.
    
29. Create an **INDEX on employee name**.
    
30. Create a **VIEW for employees with salary > 50,000**.
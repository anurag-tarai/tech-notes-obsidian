- **JPA (Java Persistence API)** is a specification for mapping Java objects to database tables.
    
- **Spring Data JPA** is a Spring framework module that simplifies JPA implementation by providing **repositories** and automatic query methods.
    
- Using JPA, you **don’t need to write SQL** for basic operations — you work with **Java objects** instead.
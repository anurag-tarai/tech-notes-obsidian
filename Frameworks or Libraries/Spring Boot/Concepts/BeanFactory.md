**BeanFactory** is a lightweight IoC container in Spring that provides basic dependency injection with lazy bean initialization.

### Key Points

- Uses **lazy initialization** (beans created only when requested)
    
- Lightweight and memory efficient
    
- Provides **basic dependency injection only**
    
- Mostly used in **simple or resource-constrained applications**
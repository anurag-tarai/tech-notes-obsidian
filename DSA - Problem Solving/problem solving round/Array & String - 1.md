### 🔹 Array (one-liners)

1. Find the maximum element in an array `Done`
    
2. Find the minimum element in an array `Done`
    
3. Reverse an array `Done`
    
4. Find duplicate elements in an array `Done`
    
5. Remove duplicates from an array `Done`
    
6. Find the second largest element `Done`
    
7. Check if an array is sorted `Done`
    
8. Count frequency of each element  `Done`
    
9. Move all zeros to the end of the array `Done`
    
10. Find the sum of all elements in an array `Done`
    
11. Find common elements between two arrays `Done`
    
12. Rotate an array by k positions `Done`
    
13. Find missing number in an array  `Done`
    
14. Merge two arrays  `Done`
    

---
### 🔹 String (one-liners)

1. Reverse a string `Done`
    
2. Check if a string is palindrome `Done`
    
3. Count frequency of characters in a string `Done`
    
4. Find first non-repeating character  `Done`
    
5. Check if two strings are anagrams `Done`
    
6. Remove duplicate characters from a string `Done`
    
7. Count vowels and consonants in a string `Done`
    
8. Find length of string without using length() `Done`
    
9. Check if string contains only digits `Done`
    
10. Count number of words in a string `Done`
    
11. Convert string to uppercase / lowercase `Done`
    
12. Find duplicate characters in a string `Done`
    
13. Replace spaces with another character `Done`
    
14. Check if string starts with a given prefix `Done`
    
15. Reverse each word in a sentence `intution is done`
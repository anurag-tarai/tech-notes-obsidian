### 🔹 AND (`&`)

Bit is `1` only if **both bits are 1**

```java
5 & 3
101
011
----
001  => 1
```

### 🔹 OR (`|`)

Bit is `1` if **any bit is 1**

```java
5 | 3
101
011
----
111  => 7
```

### 🔹 XOR (`^`) ⭐ VERY IMPORTANT

Bit is `1` if **bits are different**

```java
5 ^ 3
101
011
----
110 => 6
```

🔥 Properties of XOR:
```java
a ^ a = 0
a ^ 0 = a
```

Used in **unique number problems**.

---
### 🔹 NOT (`~`)

Flips bits:
`~5 = -(5 + 1) = -6`
![](Screenshot%20from%202026-01-22%2017-31-26.png)
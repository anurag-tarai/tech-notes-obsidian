A **Linked List** is a **linear data structure** where elements are **stored in separate nodes**, and each node contains:
- **Data**
- **Pointer (reference) to the next node**

Nodes are linked together using pointers—forming a chain-like structure.
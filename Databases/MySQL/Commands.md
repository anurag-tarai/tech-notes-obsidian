```java
SELECT * FROM users\G
```
This prints each column vertically (one per line) — super readable for wide tables

for My CLI
```java
mycli -h 3.110.186.224 -u farmer_user -p farmer_connect_db
```